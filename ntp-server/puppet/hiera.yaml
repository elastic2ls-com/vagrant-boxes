:backends:
  - yaml
:hierarchy:
  - "nodes/%{::hostname}"
  - "osfamily/%{::osfamiliy}"
  - "environment/%{::environment}"
  - "functional_groups/%{::functional_groups}"
  - "global"
:yaml:
  :datadir: "/etc/puppet/hiera"

  :logger: console
